* Add annotations / callouts on the figures
* Maybe simplify to arima model only instead of midas and forest models 
* Use streamlit or maybe voila to convert to dashboard
* Probably best output the cleaned data set to a file so we dont have to re clean every time we run the dashboard since
* I think professor wants us to verify results to other data / studies.
* Load in current data with iran war